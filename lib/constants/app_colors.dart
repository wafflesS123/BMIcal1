import 'package:flutter/material.dart';

/// All colors used in the application.
class AppColors {
  // Female Theme (Default)
  static const Color femalePrimary = Color(0xFFFF5FA2);
  static const Color femaleBackground = Color(0xFFFCE7F3);

  // Male Theme
  static const Color malePrimary = Color(0xFF4A90E2);
  static const Color maleBackground = Color(0xFFE8F1FF);

  // Common Colors
  static const Color white = Colors.white;
  static const Color black = Colors.black87;
  static const Color grey = Colors.grey;

  // BMI Gauge Colors
  static const Color underweight = Color(0xFF4FC3F7);
  static const Color normal = Color(0xFF66BB6A);
  static const Color overweight = Color(0xFFFFD54F);
  static const Color obese = Color(0xFFFF8A65);
  static const Color extremeObese = Color(0xFFE53935);
}
